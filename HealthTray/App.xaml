<Application x:Class="HealthTray.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HealthTray"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             StartupUri="DashboardWindow.xaml">
    <Application.Resources>
         
        <local:ShowDashboardCommand x:Key="ShowDashboardCommand" />
                                    
        <!-- globally-declared system tray icon -->
        <tb:TaskbarIcon x:Key="TaskbarIcon" IconSource="icons/down.ico" ToolTipText="HealthTray" DoubleClickCommand="{StaticResource ShowDashboardCommand}">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Open Dashboard" Click="TaskbarIcon_Menu_OpenDashboard_Click"/>
                    <Separator/>
                    <MenuItem Header="Exit" Click="TaskbarIcon_Menu_Exit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
        
    </Application.Resources>
</Application>
